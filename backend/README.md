# FullPrep Backend

The backend is an Express/MongoDB API under `src/`. See `../SETUP_GUIDE.md` for complete setup, environment variables, seeding, admin creation, frontend integration, and API verification.

Quick start:

```bash
npm install
npm run seed
npm run create-admin
npm run dev
```
