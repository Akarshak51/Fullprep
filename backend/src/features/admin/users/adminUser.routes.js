import express from "express";import {protect,authorizeRoles} from "../../../shared/middlewares/authMiddleware.js";import * as c from "./adminUser.controller.js";const r=express.Router();r.use(protect,authorizeRoles("admin","super_admin","moderator"));r.get("/",c.list);r.get("/export",c.exportUsers);r.patch("/:id",c.update);r.post("/:id/suspend",c.suspend);r.post("/:id/restore",c.restore);export default r;