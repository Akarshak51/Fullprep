import express from "express";import {protect,authorizeRoles} from "../../../shared/middlewares/authMiddleware.js";import * as c from "./adminProblem.controller.js";const r=express.Router();r.use(protect,authorizeRoles("admin","super_admin","moderator"));r.get("/",c.list);r.get("/:id",c.detail);r.post("/",c.createProblem);r.put("/:id",c.updateProblem);r.delete("/:id",c.deleteProblem);export default r;