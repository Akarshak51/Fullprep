import Problem from "../problems/problem.model.js";import LearningPath from "../learning-paths/learningPath.model.js";import User from "../users/user.model.js";
export async function search(q){const rx=new RegExp(q.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"i");const [problems,paths,users]=await Promise.all([Problem.find({$or:[{title:rx},{tags:rx}],isPublished:true}).limit(8).lean(),LearningPath.find({status:"published",title:rx}).limit(6).lean(),User.find({$or:[{username:rx},{name:rx}],status:"active"}).select("username name avatar").limit(6).lean()]);return {problems:problems.map(x=>({...x,id:x._id})),learningPaths:paths.map(x=>({...x,id:x._id})),users}}
