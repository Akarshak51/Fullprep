import {apiClient} from "../../../shared/services/apiClient.js";import {ENDPOINTS} from "../../../config/apiEndpoints.js";export const listLearningPaths=()=>apiClient.get(ENDPOINTS.learningPaths.list);export const getLearningPathBySlug=slug=>apiClient.get(ENDPOINTS.learningPaths.detail(slug));export const updateLearningProgress=(id,body)=>apiClient.patch(ENDPOINTS.learningPaths.progress(id),body)
