import {apiClient} from "../../../shared/services/apiClient.js";import {ENDPOINTS} from "../../../config/apiEndpoints.js";export const listAdminProblems=({search=""}={})=>apiClient.get(`${ENDPOINTS.problems.list}?search=${encodeURIComponent(search)}`);export const getAdminProblem=id=>apiClient.get(ENDPOINTS.problems.detail(id));export const saveAdminProblem=problem=>problem.id?apiClient.put(ENDPOINTS.problems.detail(problem.id),problem):apiClient.post(ENDPOINTS.problems.list,problem);export const deleteAdminProblem=id=>apiClient.del(ENDPOINTS.problems.detail(id))
