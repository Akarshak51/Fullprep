import {apiClient} from "../../../shared/services/apiClient.js";import {ENDPOINTS} from "../../../config/apiEndpoints.js";export const listAdminContests=()=>apiClient.get(ENDPOINTS.contests.list);export const getAdminContest=id=>apiClient.get(ENDPOINTS.contests.detail(id));export const saveAdminContest=contest=>contest.id?apiClient.put(ENDPOINTS.contests.detail(contest.id),contest):apiClient.post(ENDPOINTS.contests.list,contest)
