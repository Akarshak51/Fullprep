import {apiClient} from "../../../shared/services/apiClient.js";import {ENDPOINTS} from "../../../config/apiEndpoints.js";export const listReports=()=>apiClient.get(ENDPOINTS.reports.list);export const resolveReport=id=>apiClient.post(ENDPOINTS.reports.resolve(id),{})
